# Copiar a variables de entorno en Render (o a appsettings.Development.json para local)

# Cadena de conexión PostgreSQL (Supabase Session pooler)
# En Render: ConnectionStrings__DefaultConnection
# IMPORTANTE: El host varía por proyecto. Obtenerlo en Supabase: Project Settings > Database > Connection string (Session mode)
# Ejemplo sa-east-1: aws-1-sa-east-1.pooler.supabase.com (puede ser aws-0, aws-1, etc.)
ConnectionStrings__DefaultConnection=Host=aws-1-sa-east-1.pooler.supabase.com;Port=5432;Database=postgres;Username=postgres.oifvislvtkqsxelyzgyi;Password=TU_PASSWORD;SslMode=Require;Trust Server Certificate=true

# Orígenes permitidos para CORS (separados por ;)
# En Render: Cors__AllowedOrigins - DEBE incluir la URL exacta del frontend.
# Ejemplo producción: https://comanda-client.onrender.com
# Múltiples orígenes: http://localhost:5173;https://comanda-client.onrender.com
Cors__AllowedOrigins=http://localhost:5173
